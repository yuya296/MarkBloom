# Core Release Notes

## Summary
- Core packages (`@yuya296/cm6-*`) were released as a lockstep set.

## Compatibility
- Confirmed with `releases/compatibility-matrix.json`.

## Validation
- CI checks: lint, typecheck, build, test (if present), compatibility matrix.
